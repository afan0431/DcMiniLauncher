<components:ChromeWindow
    x:Class="XIVLauncher.Windows.Main.MainWindow"
    x:Name="RootWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:system="clr-namespace:System;assembly=mscorlib"
    xmlns:components="clr-namespace:XIVLauncher.Xaml.Components"
    xmlns:main="clr-namespace:XIVLauncher.Windows.Main"
    xmlns:main1="clr-namespace:XIVLauncher.Windows.ViewModel.Main"
    mc:Ignorable="d"
    Title="XIVLauncherCN (Soil)"
    Width="780"
    Height="580"
    ResizeMode="CanMinimize" WindowStartupLocation="CenterScreen"
    Icon="pack://application:,,,/Resources/dalamud_icon.ico"
    TextElement.Foreground="{DynamicResource MaterialDesign.Brush.Foreground}"
    Background="{DynamicResource MaterialDesign.Brush.Background}"
    Closing="MainWindow_OnClosing"
    d:DataContext="{d:DesignInstance main1:MainWindowViewModel}">

    <components:ChromeWindow.Resources>
        <system:Double x:Key="BannerCardWidth">450</system:Double>
        <system:Double x:Key="BannerCardHeight">250</system:Double>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />
    </components:ChromeWindow.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <components:TitleBar Grid.Row="0" />

        <Grid Grid.Row="1" Margin="10,6,10,10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition />
                </Grid.RowDefinitions>

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <!-- 注入选择 -->
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>

                <!-- 新闻轮播 -->
                <main:NewsCarouselControl x:Name="NewsCarousel"
                                          Grid.Row="0"
                                          Grid.Column="0"
                                          Width="{DynamicResource BannerCardWidth}"
                                          Height="{DynamicResource BannerCardHeight}" />

                <!-- 新闻列表 -->
                <main:NewsListControl x:Name="NewsList"
                                      Grid.Row="1"
                                      Grid.Column="0"
                                      Margin="0,12,0,4"
                                      Width="{DynamicResource BannerCardWidth}" />

                <!-- 登录卡片 -->
                <main:LoginCardControl x:Name="LoginCard"
                                       Grid.Row="0"
                                       Grid.RowSpan="2"
                                       Grid.Column="1"
                                       Width="280"
                                       Margin="8,0,0,4" />

                <!-- 本次注入（Dalamud / Minion / 都注 / 都不注）, 只在登录后的主页面显示。
                     边距与登录卡片一致, 卡片上下沿对齐；内部行结构对齐卡片内容, 见控件本体 -->
                <main:InjectionOptionsControl Grid.Row="0"
                                              Grid.RowSpan="2"
                                              Grid.Column="2"
                                              Width="240"
                                              Margin="8,0,0,4"
                                              Visibility="{Binding IsInjectionOptionsVisible, Converter={StaticResource BoolToVis}}" />

                <materialDesign:DialogHost
                    Identifier="MaintenanceQueueDialogHost"
                    x:Name="MaintenanceQueueDialogHost"
                    IsOpen="{Binding IsLoadingDialogOpen}">
                    <materialDesign:DialogHost.DialogContent>
                        <StackPanel Margin="16">
                            <StackPanel Orientation="Vertical"
                                        HorizontalAlignment="Center" VerticalAlignment="Center">
                                <TextBlock x:Name="LoadingMessageTextBlock"
                                           Text="{Binding LoadingDialogMessage}" />
                                <ProgressBar
                                    Style="{StaticResource MaterialDesignCircularProgressBar}"
                                    Value="0"
                                    IsIndeterminate="{Binding IsLoadingDialogOpen}" Height="30" Width="30"
                                    Margin="0 8 0 0" />
                            </StackPanel>
                        </StackPanel>
                    </materialDesign:DialogHost.DialogContent>
                </materialDesign:DialogHost>
            </Grid>
        </Grid>

        <materialDesign:Snackbar x:Name="CopySnackbar"
                                 Grid.Row="1"
                                 MessageQueue="{materialDesign:MessageQueue}"
                                 HorizontalAlignment="Center"
                                 VerticalAlignment="Bottom"
                                 Margin="0,0,0,16" />
    </Grid>
</components:ChromeWindow>
